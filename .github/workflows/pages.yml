name: نشر صفحة التنزيل

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: قراءة الملفات
        uses: actions/checkout@v6

      - name: إعداد GitHub Pages
        uses: actions/configure-pages@v5

      - name: تجهيز ملفات الصفحة
        uses: actions/upload-pages-artifact@v4
        with:
          path: .

      - name: نشر الصفحة
        id: deployment
        uses: actions/deploy-pages@v4
